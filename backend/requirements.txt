# --- Web framework ---
fastapi
uvicorn[standard]
python-multipart

# --- LangChain stack ---
langchain
langchain-core
langchain-community
langchain-ollama
langchain-text-splitters

# --- Vector DB + hybrid retrieval ---
# faiss را اینجا اضافه نکنید!
# در Docker، Dockerfile مدیریت نصب faiss-cpu یا faiss-gpu را بر عهده دارد (بسته به USE_CUDA).
# در محیط local:
#   CPU:  pip install faiss-cpu
#   GPU:  pip install faiss-gpu
rank-bm25

# --- Embeddings (BGE-M3 via sentence-transformers) ---
sentence-transformers
# torch را اینجا اضافه نکنید!
# در Docker، torch قبل از این فایل با index صحیح CUDA نصب می‌شود (Dockerfile).
# در محیط local:
#   CPU:  pip install torch --index-url https://download.pytorch.org/whl/cpu
#   GPU:  pip install torch --index-url https://download.pytorch.org/whl/cu124

# --- PDF loading ---
pypdf

# --- Word loading ---
python-docx
docx2txt

# --- Web crawling / search ---
requests
beautifulsoup4
ddgs
feedparser
lxml

# --- Model download helper ---
huggingface-hub